apiVersion: v1
kind: Service
metadata:
  name: my-web-app-svc
spec:
  type: ClusterIP
  selector:
    app: my-web-app
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080

